create table node (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	name varchar(256),
	host varchar(256),
	x number,
	y number
);

create table rel (
	srcNode integer,
	destNode integer,
	primary key (srcNode, destNode)
);

